<% if !@output.nil? && !@temp.nil? %>
  <h2> <%= @output %> </h2>
  The link: <%= @temp.link  %> </br>
  <%= link_to 'Show all links', root_path %>
<% else %>
  <%= "Sorry, something went wrong" %> </br>
<% end %>